# Production Environment Variables for Render Deployment
# Copy these to your Render Dashboard Environment Variables section

NODE_ENV=production

# Database Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-generate-with-crypto

# Email Configuration (Gmail with App Password)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-character-app-password
ADMIN_EMAIL=admin@yourdomain.com
INSURANCE_EMAIL=insurance@yourdomain.com

# CORS Configuration (Render will auto-set RENDER_EXTERNAL_URL)
# ALLOWED_ORIGINS will be automatically configured
# Or manually set: ALLOWED_ORIGINS=https://your-app.onrender.com

# Optional: Custom Port (Render sets this automatically)
# PORT=10000